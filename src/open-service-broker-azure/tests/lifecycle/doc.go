package lifecycle

// This file exists just to prevent the compiler from complaining that there
// are no source files in this directory at times when the unit build
// tag (which excludes all other files in this package) is set.
