language: generic
cache:
  directories:
  - .glide
services:
- docker
- docker-compose
sudo: required
before_script:
- make dev-bootstrap
script:
- make test docker-build
deploy:
  skip_cleanup: true
  provider: script
  script: scripts/deploy.sh
  on:
    repo: Azure/azure-service-broker
    all_branches: true
