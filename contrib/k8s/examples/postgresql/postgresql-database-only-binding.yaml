apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBinding
metadata:
  name: my-postgresql-database-binding
  namespace: default
spec:
  instanceRef:
    name: my-postgresql-database-instance
  secretName: my-postgresql-database-secret
